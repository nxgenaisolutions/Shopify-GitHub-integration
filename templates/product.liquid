<section class="hero" style="background-image: linear-gradient(45deg, {{ product.metafields.custom.hero_color_1 | default: '#0a4a8f' }}, {{ product.metafields.custom.hero_color_2 | default: '#1764bf' }});">
  <div class="container">
    <h1>{{ product.title }}</h1>
    <p class="tagline">{{ product.metafields.custom.tagline | default: product.description | strip_html | truncate: 200 }}</p>
  </div>
</section>

{% section 'product-features' %}
{% section 'product-details' %}
{% section 'product-consultation-form' %}

<style>
  .hero {
    color: var(--light-text-color);
    padding: 80px 0;
    text-align: center;
  }
  .hero h1 {
    font-size: 2.8rem;
    margin-bottom: 20px;
  }
  .hero .tagline {
    font-size: 1.2rem;
    max-width: 700px;
    margin: 0 auto;
    font-weight: 300;
  }
</style>